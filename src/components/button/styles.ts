import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        width: "30%",
        borderColor: '#000',
        height: 35,
        backgroundColor: "#4177a1",
        borderRadius: 10,
        justifyContent: "center",
        alignItems: "center",
        shadowColor: "#000",
        shadowOffset: { width: 0, height: 4 },
        shadowOpacity: 0.5,
        shadowRadius: 5,
        elevation: 5,
        paddingHorizontal: 10,
    },

    texto: {
        fontSize: 18,
        fontWeight: "700",
        color: "#FFF",
    }
});
