import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: "90%",
        height: 50,
        borderRadius: 10,
        borderWidth: 1.5,
        borderColor: "#000",
        padding: 10,
        fontSize: 16,
    },
})
